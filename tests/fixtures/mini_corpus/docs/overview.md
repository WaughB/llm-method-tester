# Widget Overview

Widget is a tool for managing gizmos.

## Setup

Install widget with `pip install widget`. The default port is 7777.

## Architecture

Widget runs a single coordinator process that owns all gizmo state.
